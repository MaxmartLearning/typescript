const carMakers: string[] = ["ford", "toyoya", "nisan"];

const Makers: string[] = [];

const Bakers = [];
const dates = [new Date(), new Date()];

const carsByMake: string[][] = [];

// help with 'map'
carMakers.map((car: string): string => {
  return car;
});

// multiple types
const importantDates = [new Date(), "2022-09-27", 25];
